# ── Development tools (local machine only) ──────────────────────────────────
ruff>=0.5.0           # fast linter + formatter
mypy>=1.10.0          # static type checker
packaging>=23.0       # version parsing (used by torch version guard)

# ── Core dependencies (installed on RunPod via notebook Cell 1) ─────────────
# torch>=2.6.0
# torchvision>=0.21.0
# typing_extensions>=4.10.0
# transformers>=4.40.0
# datasets>=2.18.0
# accelerate>=0.27.0
# huggingface_hub>=0.21.0
# scikit-learn>=1.3.0
# matplotlib>=3.8.0
# seaborn>=0.13.0
# pandas>=2.1.0
# tqdm
# medmnist              # Part 4 only
# tensorflow>=2.15.0    # Part 2 only
